<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#0A0A0F"
    tools:context=".Screens.GamesList">

    <!-- כותרת המסך -->
    <TextView
        android:id="@+id/textViewListTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="רשימת משחקים"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#F0E6FF"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/card_bg_dark"
        android:padding="4dp"
        android:layout_marginBottom="12dp">

        <!-- שדה סינון לפי מספר קבוצה -->
        <EditText
            android:id="@+id/editTextTeamFilter"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:imeOptions="actionDone"
            android:hint="סנן לפי מספר קבוצה"
            android:inputType="number"
            android:textColor="#F0E6FF"
            android:textColorHint="#4A3F5C"
            android:backgroundTint="#2A1F3D"
            android:padding="12dp"
            android:layout_marginEnd="8dp" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/gamesList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_margin="10dp" />

</LinearLayout>